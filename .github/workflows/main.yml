name: Windows Simple Workflow

on: [push]

jobs:
  say-hello:
    runs-on: windows-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Say Hello on Windows
        run: echo "Hello from GitHub Actions on Windows!"
